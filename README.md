# abalone_Regression_Using_Normal_Equation

predict the column rings (output) using other columns as input features. there are  two files, one contains the data and the other contains the column names
